org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental caches use memory-mapped files that it cannot reliably
# close on Windows, which fails the build of plugin modules with
# "Could not close incremental caches". Only plugin Kotlin is affected — the
# app itself is Dart — so the rebuild cost of turning this off is negligible.
kotlin.incremental=false
