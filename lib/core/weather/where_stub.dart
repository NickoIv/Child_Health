import 'place.dart';

/// No location off the web yet — see `where.dart`.
///
/// Null rather than an exception: a question about a walk is still answered,
/// just without the figures.
Future<Place?> currentPlace() async => null;
